{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "OTA: Dev",
            "type": "shell",
            "command": "pio run -t otaprep --environment dev"
        },
        {
            "label": "OTA: Prod",
            "type": "shell",
            "command": "pio run -t otaprep --environment prod"
        }
    ]
}